{
	"$schema": "https://turborepo.com/schema.json",
	"tasks": {
		"build:worldbase": {
			"inputs": ["src/**"],
			"outputs": ["dist/@World/**"]
		},
		"build:server": {
			"inputs": ["src/**"],
			"dependsOn": ["build:worldbase"],
			"outputs": ["dist/server/**"]
		},
		"build:client": {
			"inputs": ["src/**"],
			"dependsOn": ["build:worldbase"],
			"outputs": ["dist/client/**"]
		},
		"build:offline": {
			"inputs": ["src/**"],
			"dependsOn": ["build:worldbase"],
			"outputs": ["dist/offline/**"]
		},
		"build_struct": {
			"dependsOn": ["build:server", "build:client"],
			"outputs": []
		},
		"dev": {
			"persistent": true,
			"cache": false
		}
	}
}
