# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js socketControl

on:
  push:
    branches: ['main-v-0.5']
  pull_request:
    branches: ['main-v-0.5']

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [24.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Building:World
        run: npm run build:world
      - name: Building:WorldClient
        run: npm run build:worldclient
      - name: Building:WorldServer
        run: npm run build:worldserver
      - name: Building:Offline
        run: npm run build:offline
      - name: Building:Client
        run: npm run build:client
      - name: Building:Server
        run: npm run build:server

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
